name: Testando github action

on: [push, pull_request]

jobs:
  linux_job:

    runs-on: ubuntu-latest

    steps: 
      - name: Executa echo
        run: echo "Hello world!" > tmp.txt
      - name: Executa pls
        run: ls

  windows_job:

    runs-on: windows-latest

    steps: 
      - name: Lista dir
        run: dir